<%= turbo_frame_tag task do %>
  <%= bootstrap_form_with(model: task, data: { action: "turbo:submit-end->modal#close" }) do |form| %>
    <%= form.text_field :title %>
    <%= form.text_area :body %>
    <%= form.primary %>
  <% end%>
<% end %>
